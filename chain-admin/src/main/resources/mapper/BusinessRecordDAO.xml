<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 交易记录表 -->
<mapper namespace="com.ynmarry.chain.dao.BusinessRecordDAO">
    <sql id="allColumns">
        t.id as id, t.user_id as userId, t.type as type, t.pay_money as payMoney, t.created as created, t.updated as updated
    </sql>
</mapper>