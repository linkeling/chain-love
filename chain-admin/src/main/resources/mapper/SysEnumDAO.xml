<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 枚举信息 -->
<mapper namespace="com.ynmarry.chain.dao.SysEnumDAO">
    <sql id="allColumns">
        t.id as id, t.enum_name as enumName, t.table_name as tableName, t.fields_name as fieldsName, t.valid_status as validStatus, t.created as created, t.updated as updated
    </sql>
</mapper>